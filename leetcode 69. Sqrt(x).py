import math

class Solution:
    def mySqrt(self, x: int) -> int:
        answer = int(math.sqrt(x))   # 제곱근을 구하는 함수인 Math.sqrt()를 사용하여 그 값을 소수점을 없애기 위해 int()를 취해줍니다
        return answer
    
    
    

